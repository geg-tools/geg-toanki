[project]
name = "geg-toanki"
version = "0.4.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gabriel Guerra", email = "gabrielcake9@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "google-genai>=2.19.0",
    "requests>=2.34.2",
    "typer>=0.27.1",
]

[project.scripts]
geg-toanki = "geg_toanki.cli:app"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
